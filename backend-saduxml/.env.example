# Server
PORT=5000
APP_URL=http://localhost:5000

# MySQL
DB_HOST=localhost
DB_USER=root
DB_PASS=secret
DB_NAME=eventsdb

# JWT
JWT_SECRET=please_change_this

# SMTP (optional)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="no-reply@example.com"
